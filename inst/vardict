# Variable dictionaries 

- img$hex_plot: image that is actually drawn on the hex, on the left
- img$original_image: image that has been uploaded, horns at the beginning, img at restoration
- r$modal_plot: image that is inside the modal
